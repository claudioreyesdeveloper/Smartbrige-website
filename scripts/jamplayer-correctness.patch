diff --git a/components/band-jam/practice-screen.tsx b/components/band-jam/practice-screen.tsx
index 3a71674..8e1457d 100644
--- a/components/band-jam/practice-screen.tsx
+++ b/components/band-jam/practice-screen.tsx
@@ -30,6 +30,10 @@ import {
 } from "@/lib/band-jam/engine/reharmonization"
 import { applyJamPlayerFreeTier } from "@/lib/band-jam/free-tier"
 import { BandPlayer } from "@/lib/band-jam/engine/player"
+import {
+  effectivePartMuted,
+  elapsedBeatsForAudioTime,
+} from "@/lib/band-jam/engine/playback-state"
 import {
   EffectsRack,
   type PartEffectSettings,
@@ -103,6 +107,11 @@ function isPartDisabled(
  * later, leaving a short but audible window in which a section click could
  * start the previous variation.
  */
+type ArrangementBuildResult = {
+  arrangement: Arrangement | null
+  error: string | null
+}
+
 function buildPlayableArrangement({
   style,
   progression,
@@ -123,8 +132,10 @@ function buildPlayableArrangement({
   styleId: string
   partPlan?: SectionPartPlan | null
   includeSectionFills?: boolean
-}): Arrangement | null {
-  if (!style || !progression || !clips) return null
+}): ArrangementBuildResult {
+  if (!style || !progression || !clips) {
+    return { arrangement: null, error: null }
+  }
   try {
     const out = arrange({
       style,
@@ -135,16 +146,21 @@ function buildPlayableArrangement({
       variation,
       includeSectionFills,
     })
-    if (partPlan) return applySectionPartPlan(out, partPlan)
-    return {
-      ...out,
-      parts: out.parts.filter(
-        (part) => !isPartDisabled(part.part, styleId, variation, style),
-      ),
-    }
+    const arrangement = partPlan
+      ? applySectionPartPlan(out, partPlan)
+      : {
+          ...out,
+          parts: out.parts.filter(
+            (part) => !isPartDisabled(part.part, styleId, variation, style),
+          ),
+        }
+    return { arrangement, error: null }
   } catch (err) {
     console.error("arrange failed", err)
-    return null
+    return {
+      arrangement: null,
+      error: "This arrangement could not be built. Try another song or style.",
+    }
   }
 }
 
@@ -199,6 +215,7 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
   const [catalog, setCatalog] = useState<CatalogJson | null>(null)
   const [clips, setClips] = useState<Map<number, { events: NoteEvent[]; sourceKeyPc: number }> | null>(null)
   const [loadError, setLoadError] = useState("")
+  const [instrumentError, setInstrumentError] = useState("")
   const [progress, setProgress] = useState<number | null>(null)
 
   const [styleId, setStyleId] = useState<string>("")
@@ -240,13 +257,13 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
   const practiceRef = useRef<PracticeStore>({})
   /** Beats accumulated since the current span started, for ramp + history. */
   const spanBeatsRef = useRef(0)
-  const spanStartBeatRef = useRef(0)
   const spanStartTempoRef = useRef(0)
 
   const playerRef = useRef<BandPlayer | null>(null)
   const midiSchedRef = useRef<MidiScheduler | null>(null)
   const effectsRef = useRef<EffectsRack | null>(null)
   const mixRef = useRef(mix)
+  const soloedRef = useRef<BandPart | null>(null)
   const eqRef = useRef(channelEq)
   const sendsRef = useRef(sends)
   const panRef = useRef(channelPan)
@@ -281,6 +298,7 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
   const instrumentLoadRevisionRef = useRef(0)
   const styleIdRef = useRef(styleId)
   styleIdRef.current = styleId
+  soloedRef.current = soloed
   loopRef.current = loop
   tempoRef.current = tempo
   countInRef.current = countIn
@@ -309,11 +327,15 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
     setMixDirty(false)
     setMixJustSaved(false)
     setSoloed(null)
+    soloedRef.current = null
     setUserPart(null)
     const player = playerRef.current
+    const midiScheduler = midiSchedRef.current
     for (const part of ALL_PARTS) {
-      player?.setMuted(part, next.mix[part].muted)
+      const muted = effectivePartMuted(part, next.mix, null)
+      player?.setMuted(part, muted)
       player?.setVolume(part, next.mix[part].volume)
+      midiScheduler?.setPartEnabled(part, !muted)
       effectsRef.current?.setPartUserEq(part, next.eq[part])
       effectsRef.current?.setPartPan(part, next.pan[part])
       effectsRef.current?.setReverbSend(part, next.sends[part])
@@ -379,7 +401,7 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
       } catch (err) {
         if (!cancelled) {
           setLoadError(
-            "Catalogue missing. Run scripts/band_jam_pilot/export_catalog.py and export_clips.py.",
+            "The Jam Player catalogue could not be loaded. Check the connection and try again.",
           )
           console.error(err)
         }
@@ -427,7 +449,7 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
     setArrangerJustSaved(false)
   }, [style, defaultArranger])
 
-  const arrangement: Arrangement | null = useMemo(() => {
+  const arrangementBuild = useMemo(() => {
     // Tempo is stamped on the arrangement for consumers that read it, but it
     // must NOT be a useMemo dependency: every tempo tick (slider / ramp)
     // rebuilt this object and disturbed live playback.
@@ -443,8 +465,9 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
     })
     // eslint-disable-next-line react-hooks/exhaustive-deps -- tempo is live via setTempo
   }, [style, activeProgression, clips, keyPc, variation, styleId, effectiveArranger])
+  const arrangement = arrangementBuild.arrangement
 
-  const sectionArrangement: Arrangement | null = useMemo(() => {
+  const sectionArrangementBuild = useMemo(() => {
     return buildPlayableArrangement({
       style,
       progression: activeProgression,
@@ -458,6 +481,8 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
     })
     // eslint-disable-next-line react-hooks/exhaustive-deps -- tempo is live via setTempo
   }, [style, activeProgression, clips, keyPc, variation, styleId, effectiveArranger])
+  const sectionArrangement = sectionArrangementBuild.arrangement
+  const arrangementError = arrangementBuild.error ?? sectionArrangementBuild.error
 
   // Event handlers must always see the arrangement represented by the current
   // screen, even before passive effects have run.
@@ -525,11 +550,17 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
       readyRef.current &&
       !instrumentsStaleRef.current &&
       loadedStyleRef.current === styleId
-    )
+    ) {
+      const ctx = ctxRef.current
+      if (ctx && midiLive && midi.midiOut && !midiSchedRef.current) {
+        midiSchedRef.current = new MidiScheduler(ctx, midi.midiOut)
+      }
       return playerRef.current
+    }
     if (!arrangement) return null
 
     setStatus("loading")
+    setInstrumentError("")
     try {
       const ctx = ctxRef.current ?? new AudioContext()
       ctxRef.current = ctx
@@ -708,8 +739,8 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
       return player
     } catch (err) {
       console.error(err)
-      setLoadError(
-        "Instruments failed to load. Run scripts/band_jam_pilot/build_web_samples.py.",
+      setInstrumentError(
+        "The instrument samples could not be loaded. Check the connection and try again.",
       )
       setStatus("idle")
       setProgress(null)
@@ -717,6 +748,15 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
     }
   }, [arrangement, styleId, style, midiLive, midi.midiOut])
 
+  const applyPlaybackTempo = useCallback((bpm: number) => {
+    const next = Math.max(20, Math.min(300, bpm))
+    tempoRef.current = next
+    playerRef.current?.setTempo(next)
+    midiSchedRef.current?.setTempo(next)
+    effectsRef.current?.setTempo(next)
+    setTempo(next)
+  }, [])
+
   /**
    * Commit one complete playback pass to both audio and MIDI.
    *
@@ -745,7 +785,12 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
     player.setCountInBars(liveCountIn ? 1 : 0)
     player.setMetronome(liveMetronome)
     for (const part of ALL_PARTS) {
-      player.setMuted(part, mixRef.current[part].muted)
+      const muted = effectivePartMuted(
+        part,
+        mixRef.current,
+        soloedRef.current,
+      )
+      player.setMuted(part, muted)
       player.setVolume(part, mixRef.current[part].volume)
     }
     if (options.startBar !== undefined) player.seekToBar(options.startBar)
@@ -757,7 +802,12 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
       ms.setLoop(options.range)
       ms.setCountInBars(liveCountIn ? 1 : 0)
       for (const part of ALL_PARTS) {
-        ms.setPartEnabled(part, !mixRef.current[part].muted)
+        const muted = effectivePartMuted(
+          part,
+          mixRef.current,
+          soloedRef.current,
+        )
+        ms.setPartEnabled(part, !muted)
       }
       if (options.startBar !== undefined) ms.seekToBar(options.startBar)
       if (options.resume) void ms.play()
@@ -796,42 +846,48 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
 
   useEffect(() => {
     let raf = 0
-    let lastBeat: number | null = null
+    let lastAudioTime: number | null = null
+    let wasCountingIn = false
     const tick = () => {
       const p = playerRef.current
       if (p && p.getStatus() === "playing") {
+        const displayBeat = p.getCurrentBeat()
         setCurrentBar(p.getCurrentBar())
-        setBarPhase((p.getCurrentBeat() % 4) / 4)
-        // Accumulate elapsed beats. getCurrentBeat wraps at the loop, so count
-        // forward deltas only and treat a wrap as a fresh delta rather than a
-        // large negative one.
-        const b = p.getCurrentBeat()
-        if (lastBeat !== null) {
-          const d = b - lastBeat
-          spanBeatsRef.current += d >= 0 ? d : Math.max(0, b)
+        setBarPhase((displayBeat % 4) / 4)
+
+        // AudioContext time is monotonic even when the visible playhead folds
+        // back to the start of a loop. This keeps practice history and Tempo
+        // Trainer progress correct for loops that start after bar one.
+        const audioTime = ctxRef.current?.currentTime ?? null
+        const countingIn = p.isCountingIn()
+        if (audioTime !== null) {
+          spanBeatsRef.current += elapsedBeatsForAudioTime(
+            lastAudioTime,
+            audioTime,
+            p.getTempo(),
+            countingIn || wasCountingIn,
+          )
+          lastAudioTime = audioTime
         }
-        lastBeat = b
+        wasCountingIn = countingIn
 
         if (rampOn && targetTempo) {
           const next = rampTempo(spanStartTempoRef.current, spanBeatsRef.current, {
             ...DEFAULT_RAMP,
             targetTempo,
           })
-          if (next !== p.getTempo()) {
-            p.setTempo(next)
-            effectsRef.current?.setTempo(next)
-            setTempo(next)
-          }
+          if (next !== p.getTempo()) applyPlaybackTempo(next)
         }
       } else {
-        lastBeat = null
+        lastAudioTime = null
+        wasCountingIn = false
         setBarPhase(0)
       }
       raf = requestAnimationFrame(tick)
     }
     raf = requestAnimationFrame(tick)
     return () => cancelAnimationFrame(raf)
-  }, [rampOn, targetTempo])
+  }, [rampOn, targetTempo, applyPlaybackTempo])
 
   useEffect(
     () => () => {
@@ -840,6 +896,13 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
       // A hung note on real hardware outlives the page, so always stop the
       // MIDI sink explicitly on unmount.
       midiSchedRef.current?.stop()
+      const ctx = ctxRef.current
+      ctxRef.current = null
+      if (ctx && ctx.state !== "closed") {
+        void ctx.close().catch((err) => {
+          console.error("AudioContext close failed", err)
+        })
+      }
     },
     [],
   )
@@ -970,31 +1033,36 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
     // eslint-disable-next-line react-hooks/exhaustive-deps
   }, [styleId])
 
-  const applyMix = (next: Record<BandPart, PartMixState>) => {
-    setMix(next)
-    mixRef.current = next
+  const syncMixToOutputs = (
+    next: Record<BandPart, PartMixState>,
+    activeSolo: BandPart | null,
+  ) => {
     const player = playerRef.current
-    if (!player) return
+    const midiScheduler = midiSchedRef.current
     for (const part of ALL_PARTS) {
-      player.setMuted(part, next[part].muted)
-      player.setVolume(part, next[part].volume)
+      const muted = effectivePartMuted(part, next, activeSolo)
+      player?.setMuted(part, muted)
+      player?.setVolume(part, next[part].volume)
+      midiScheduler?.setPartEnabled(part, !muted)
     }
   }
 
+  const applyMix = (next: Record<BandPart, PartMixState>) => {
+    setMix(next)
+    mixRef.current = next
+    syncMixToOutputs(next, soloedRef.current)
+  }
+
   /** Solo mutes everything else without destroying the user's own mute state. */
   const applySolo = (part: BandPart | null) => {
     setSoloed(part)
-    const player = playerRef.current
-    if (!player) return
-    for (const p of ALL_PARTS) {
-      const muted = part ? p !== part : (mix[p]?.muted ?? false)
-      player.setMuted(p, muted)
-    }
+    soloedRef.current = part
+    syncMixToOutputs(mixRef.current, part)
   }
 
   const pickUserPart = (part: BandPart | null) => {
     setUserPart(part)
-    const next = { ...mix }
+    const next = { ...mixRef.current }
     for (const p of ALL_PARTS) {
       next[p] = { ...next[p], muted: p === part ? true : p === "solo" ? next[p].muted : false }
     }
@@ -1002,13 +1070,21 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
     applyMix(next)
   }
 
-  if (loadError) {
+  const visibleError = loadError || arrangementError || instrumentError
+  if (visibleError) {
     return (
       <div className="pt-32 pb-16">
         <div className="content-wrap">
-          <p className="rounded-xl border border-rose-500/30 bg-rose-500/10 p-4 text-sm text-rose-200">
-            {loadError}
-          </p>
+          <div className="rounded-xl border border-rose-500/30 bg-rose-500/10 p-4 text-sm text-rose-200">
+            <p>{visibleError}</p>
+            <button
+              type="button"
+              onClick={() => window.location.reload()}
+              className="mt-3 rounded-lg border border-rose-200/25 px-3 py-2 text-xs font-medium text-rose-100 transition hover:bg-rose-100/10"
+            >
+              Retry Jam Player
+            </button>
+          </div>
         </div>
       </div>
     )
@@ -1042,10 +1118,7 @@ export function PracticeScreen({ hasFullAccess = false }: PracticeScreenProps =
       setCurrentBar(null)
     },
     onTempo: (bpm: number) => {
-      setTempo(bpm)
-      playerRef.current?.setTempo(bpm)
-      midiSchedRef.current?.setTempo(bpm)
-      effectsRef.current?.setTempo(bpm)
+      applyPlaybackTempo(bpm)
       spanStartTempoRef.current = bpm
       spanBeatsRef.current = 0
     },
diff --git a/lib/band-jam/engine/playback-state.test.ts b/lib/band-jam/engine/playback-state.test.ts
new file mode 100644
index 0000000..71b98ae
--- /dev/null
+++ b/lib/band-jam/engine/playback-state.test.ts
@@ -0,0 +1,40 @@
+import { describe, expect, it } from "vitest"
+import type { BandPart, PartMixState } from "@/lib/band-jam/engine/types"
+import {
+  effectivePartMuted,
+  elapsedBeatsForAudioTime,
+} from "@/lib/band-jam/engine/playback-state"
+
+const mix: Record<BandPart, PartMixState> = {
+  drums: { volume: 1, muted: false },
+  bass: { volume: 1, muted: true },
+  guitar: { volume: 0.8, muted: false },
+  keys: { volume: 0.75, muted: false },
+  solo: { volume: 0.8, muted: true },
+}
+
+describe("effectivePartMuted", () => {
+  it("uses the saved mute state when no part is soloed", () => {
+    expect(effectivePartMuted("bass", mix, null)).toBe(true)
+    expect(effectivePartMuted("guitar", mix, null)).toBe(false)
+  })
+
+  it("uses the same solo mask for browser audio and Web MIDI", () => {
+    expect(effectivePartMuted("guitar", mix, "guitar")).toBe(false)
+    expect(effectivePartMuted("drums", mix, "guitar")).toBe(true)
+    expect(effectivePartMuted("bass", mix, "guitar")).toBe(true)
+  })
+})
+
+describe("elapsedBeatsForAudioTime", () => {
+  it("counts monotonic elapsed beats independently of a folded loop playhead", () => {
+    expect(elapsedBeatsForAudioTime(10, 12, 120, false)).toBe(4)
+  })
+
+  it("does not count first samples, count-in, invalid tempo, or backwards time", () => {
+    expect(elapsedBeatsForAudioTime(null, 12, 120, false)).toBe(0)
+    expect(elapsedBeatsForAudioTime(10, 12, 120, true)).toBe(0)
+    expect(elapsedBeatsForAudioTime(10, 12, 0, false)).toBe(0)
+    expect(elapsedBeatsForAudioTime(12, 10, 120, false)).toBe(0)
+  })
+})
diff --git a/lib/band-jam/engine/playback-state.ts b/lib/band-jam/engine/playback-state.ts
new file mode 100644
index 0000000..1435b0a
--- /dev/null
+++ b/lib/band-jam/engine/playback-state.ts
@@ -0,0 +1,27 @@
+import type { BandPart, PartMixState } from "@/lib/band-jam/engine/types"
+
+/** Resolve the one effective mute state shared by browser audio and Web MIDI. */
+export function effectivePartMuted(
+  part: BandPart,
+  mix: Record<BandPart, PartMixState>,
+  soloed: BandPart | null,
+): boolean {
+  return soloed !== null ? part !== soloed : mix[part].muted
+}
+
+/**
+ * Convert a monotonic AudioContext-time delta to elapsed musical beats.
+ * The visible arrangement playhead may wrap at a loop; AudioContext time does not.
+ */
+export function elapsedBeatsForAudioTime(
+  previousTime: number | null,
+  currentTime: number,
+  tempo: number,
+  countingIn: boolean,
+): number {
+  if (previousTime === null || countingIn) return 0
+  if (!Number.isFinite(previousTime) || !Number.isFinite(currentTime)) return 0
+  if (!Number.isFinite(tempo) || tempo <= 0) return 0
+  const elapsedSeconds = Math.max(0, currentTime - previousTime)
+  return elapsedSeconds * (tempo / 60)
+}
